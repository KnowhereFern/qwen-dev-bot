---
description: Run the project's structured Qwen multi-agent implementation workflow.
---

Invoke the workflow tool with `.qwen/workflows/harness-implement.js` and pass the user's remaining arguments as workflow args. Respect `AUTONOMY.md`; only the implementer may edit and independent agents must remain read-only.
